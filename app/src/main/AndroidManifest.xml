<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="circle.fhw.com.friendcircle">
    <uses-permission   android:name ="android.permission.INTERNET"  />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="com.fhw.AppContext"
        android:theme="@style/AppTheme">
        <activity android:name="com.fhw.activity.friendCircle.FriendCirclesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.fhw.activity.showPicture.ShowPictureActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.Transparent"
            />

    </application>

</manifest>